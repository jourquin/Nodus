# Nodus plugin example

This directory can contain a simple Nodus plugin that will be loaded at launch time.

The source code of a plugin is provided. You need a JDK to compile
it using the "compile" script.